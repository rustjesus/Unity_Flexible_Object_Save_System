K to save
L to load
Add "Saveable" to objects you want to save then add the path AFTER "Resources" directory so example: if path Resources/AnotherDir/PrefabName then "AnotherDir/PrefabName" as prefabPath string, then add savesystem to scene and it works!
