K to save
L to load
Add "Saveable" to objects you want to save then add the path AFTER "Resources" directory so example: if path Assets/Resources/AnotherDir/PrefabName then "AnotherDir/PrefabName" as prefabPath string (Everything after resources directory), 
then add savesystem to scene and it works!
